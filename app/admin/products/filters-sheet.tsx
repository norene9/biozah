"use client";

export function FiltersSheet({ open, category, setCategory, status, setStatus, sort, setSort, categories, onClose, onClear }: { open: boolean; category: string; setCategory: (value: string) => void; status: string; setStatus: (value: string) => void; sort: string; setSort: (value: string) => void; categories: { id: string; name: string }[]; onClose: () => void; onClear: () => void }) { if (!open) return null; return <div className="filters-sheet-overlay" onMouseDown={(event) => event.target === event.currentTarget && onClose()}><section className="filters-sheet" role="dialog" aria-modal="true" aria-label="Product filters"><div className="filters-sheet-heading"><h2>Filters</h2><button type="button" className="icon-button" aria-label="Close filters" onClick={onClose}>×</button></div><label>Collection<select value={category} onChange={(event) => setCategory(event.target.value)}><option value="all">All collections</option>{categories.map((item) => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label><label>Status<select value={status} onChange={(event) => setStatus(event.target.value)}><option value="all">All status</option><option value="active">Active</option><option value="inactive">Inactive</option><option value="featured">Featured</option><option value="out">Out of stock</option></select></label><label>Sort<select value={sort} onChange={(event) => setSort(event.target.value)}><option value="newest">Newest</option><option value="name">Name A-Z</option><option value="price">Price low-high</option><option value="stock">Stock low-high</option></select></label><div className="filters-sheet-actions"><button type="button" className="text-link" onClick={onClear}>Clear</button><button type="button" className="button button-dark" onClick={onClose}>Apply</button></div></section></div>; }

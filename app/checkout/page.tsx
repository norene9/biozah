"use client";
import { FormEvent, useState } from "react";
import { useCart } from "@/components/cart-provider";
export default function CheckoutPage() { const { items, subtotal } = useCart(); const [error, setError] = useState(""); const [busy, setBusy] = useState(false); async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(""); const data = Object.fromEntries(new FormData(event.currentTarget)); const response = await fetch("/api/orders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ ...data, items: items.map(({ product, quantity }) => ({ product_id: product.id, quantity })) }) }); const result = await response.json(); if (!response.ok) { setError(result.error ?? "We could not create your order."); setBusy(false); return; } window.location.href = `/order-confirmed?order=${result.order_id}`; } if (!items.length) return <main><section className="empty-state"><h1>Your bag is empty.</h1><p>Add products before heading to checkout.</p></section></main>; return <main><section className="page-intro"><p className="eyebrow">Almost there</p><h1>Let&apos;s get it to you.</h1></section><section className="checkout-layout"><form className="checkout-form" onSubmit={submit}><h2>Delivery details</h2><label>Full name<input name="customer_name" required /></label><label>Email<input name="email" type="email" required /></label><label>Phone<input name="phone" required /></label><div className="form-row"><label>Wilaya<input name="wilaya" required /></label><label>Commune<input name="commune" required /></label></div><label>Full address<textarea name="address" required rows={3} /></label><h2>Delivery & payment</h2><label>Delivery method<select name="delivery_method" defaultValue="home"><option value="home">Home delivery · 600 DZD</option><option value="pickup">Office / pickup point · 350 DZD</option></select></label><label>Payment method<select name="payment_method" defaultValue="cash"><option value="cash">Cash on delivery</option><option value="transfer">Bank transfer</option></select></label>{error && <p className="form-error">{error}</p>}<button className="button button-dark" disabled={busy}>{busy ? "Placing order..." : "Place order"}</button></form><aside className="summary"><h2>Order summary</h2><div><span>Items</span><span>{subtotal.toLocaleString()} DZD</span></div><div><span>Delivery</span><span>600 DZD</span></div><div className="summary-total"><span>Total</span><strong>{(subtotal + 600).toLocaleString()} DZD</strong></div></aside></section></main>; }
